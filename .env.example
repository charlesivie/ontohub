# Copy this file to .env and fill in the values.
# Never commit .env — it is listed in .gitignore.

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development

# ─── Session ──────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
SESSION_SECRET=

# ─── GraphDB ──────────────────────────────────────────────────────────────────
GRAPHDB_URL=http://localhost:7200
GRAPHDB_REPOSITORY=ontohub

# ─── GitHub OAuth App ─────────────────────────────────────────────────────────
# Create at: https://github.com/settings/developers > OAuth Apps
# Homepage URL:      http://localhost:3000
# Callback URL:      http://localhost:3001/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3001/auth/github/callback

# ─── Webhook Secret Encryption ────────────────────────────────────────────────
# AES-256-GCM key used to encrypt per-repo webhook secrets before storing in GraphDB.
# Generate with: openssl rand -hex 32
# WARNING: if this key is lost, all stored webhook secrets become unreadable
# and every registered repo will need its webhook recreated.
WEBHOOK_ENCRYPTION_KEY=

# ─── Webhook Delivery URL ─────────────────────────────────────────────────────
# The publicly reachable base URL of this Express server, used when installing
# webhooks on GitHub repos. GitHub must be able to POST to this address.
#
# Local development: localhost is not reachable by GitHub. Use a tunnel:
#   npx ngrok http 3001   →  copy the https URL here
#
# Production: set to your actual domain (e.g. https://api.ontohub.org)
WEBHOOK_BASE_URL=https://YOUR_NGROK_URL_OR_DOMAIN
