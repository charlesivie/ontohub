# Copy this file to web/.env.local and fill in the values.
# Never commit .env.local — it is listed in .gitignore.

# ─── Express Backend ──────────────────────────────────────────────────────────
# Used server-side in Next.js (Server Components, Route Handlers, rewrites).
API_URL=http://localhost:3001

# Used client-side in Next.js (Client Components).
# Must be prefixed NEXT_PUBLIC_ to be exposed to the browser bundle.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
